<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/homeFragment"
            android:icon="@drawable/ic_tab_home"
            android:title="@string/tab_home"
            android:contentDescription="drawer_home" />
        <item
            android:id="@+id/gesturesFragment"
            android:title="@string/tab_gestures"
            android:contentDescription="drawer_gestures" />
        <item
            android:id="@+id/alertsFragment"
            android:title="@string/tab_alerts"
            android:contentDescription="drawer_alerts" />
        <item
            android:id="@+id/profileFragment"
            android:title="@string/tab_settings"
            android:contentDescription="drawer_settings" />
        <item
            android:id="@+id/webViewFragment"
            android:title="@string/tab_web"
            android:contentDescription="drawer_web" />
        <item
            android:id="@+id/systemFragment"
            android:title="@string/tab_system"
            android:contentDescription="drawer_system" />
    </group>

    <!-- Logout is outside the checkable group — it's an action, not a destination. -->
    <item
        android:id="@+id/drawer_logout"
        android:title="@string/drawer_logout"
        android:contentDescription="drawer_logout" />
</menu>
